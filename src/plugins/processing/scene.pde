// This file is for displaying the results, not for messing with them!

void setup() 
{
  size(640, 360); 
  noStroke();
  rectMode(CENTER);
}
  
void draw() 
{   
  background(105); 
  fill(255, 0,204);
  textSize(44);
  text("No illustration loaded");
}
